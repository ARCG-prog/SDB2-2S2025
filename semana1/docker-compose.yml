services:
  myDB:
    image: postgres:15.3
    container_name: my-database
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=fernando
      - POSTGRES_PASSWORD=S8DB2S2025
      - POSTGRES_DB=bases2-db
    volumes:
      - ./postgres:/var/lib/postgresql/data
      - ./pgbackrest_repo:/var/lib/pgbackrest       # Carpeta para backups
      - ./pgbackrest_conf:/etc/pgbackrest           # Configuración de pgBackRest
      - C:/Users/aleja/Downloads:/var/lib/postgresql/data_import
